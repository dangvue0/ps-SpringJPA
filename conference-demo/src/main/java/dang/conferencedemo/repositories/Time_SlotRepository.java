package dang.conferencedemo.repositories;

import dang.conferencedemo.models.Time_Slot;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Time_SlotRepository extends JpaRepository<Time_Slot, Long> {
}
